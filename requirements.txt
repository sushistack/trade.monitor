python-binance==1.0.19
httpx==0.27.2
pandas==2.2.3
mplfinance==0.12.10b0
Pillow==10.4.0
pydantic==2.9.2
pydantic-settings==2.6.1
PyYAML==6.0.2
structlog==24.4.0
# python-crontab dropped (Story 5.6): k8s CronJob owns scheduling, not in-container cron.
yfinance>=1.4.1
# requests is imported directly (uploader.py, awtrix_client.py) — was only satisfied transitively
# via python-binance/yfinance. Declare it so the container build can't silently lose it.
requests>=2.31,<3
